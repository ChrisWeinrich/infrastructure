# Contracts

No external API contracts are defined for this feature. Automation is executed
via Ansible playbooks and shell entry scripts and does not expose network APIs
that require contract definitions.
