# Server Baselines

This directory indexes the baseline documentation for each server.

## Naming

- Use one Markdown file per server: `docs/servers/<hostname>.md`
- Ensure the filename matches the hostname in `ansible/configs/base-config.yml`

## Required Content

Each server baseline file must include:

- MAC address
- Server configuration summary
- Base server information
